// Single source of truth for the production origin. Every canonical URL, sitemap
// entry, JSON-LD node, OG tag, and llms.txt link derives from this — update here
// (or point it at a custom domain) and it propagates across the whole site.
export const SITE_URL = "https://aishwaryaganesan.vercel.app";
